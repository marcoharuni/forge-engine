#!/usr/bin/env bash
set -euo pipefail

# TODO: Select and invoke benchmark entry points after benchmark implementations exist.
echo "Benchmark implementations are not available in the scaffold." >&2
exit 2

