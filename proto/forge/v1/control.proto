syntax = "proto3";

package forge.v1;

message CancellationRequest {
  string request_id = 1;
  string reason = 2;
}

message CancellationResult {
  string request_id = 1;
  bool accepted = 2;
}

service ControlService {
  rpc Cancel(CancellationRequest) returns (CancellationResult);
}

