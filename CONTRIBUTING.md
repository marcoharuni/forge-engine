# Contributing

Thank you for contributing to ForgeEngine.

## Development workflow

Use Python 3.12 and `uv` for Python commands, stable Rust for the gateway workspace, and
CMake for optional native components. See `docs/development/` for environment, build,
testing, and toolchain notes.

Before submitting a change, run `make check` and document any unavailable optional GPU
validation.

