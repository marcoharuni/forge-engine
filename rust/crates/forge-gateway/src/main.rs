//! Standalone Rust gateway process entry point.

mod config;
mod error;
mod routes;
mod state;
mod streaming;

// TODO: Assemble configuration, telemetry, routing, and transport without embedding Python.
fn main() {}
